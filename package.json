{
  "name": "certificate-verification-system",
  "version": "1.0.0",
  "description": "Certificate Verification System with backend API",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "js-beautify": "^1.15.4"
  }
}
